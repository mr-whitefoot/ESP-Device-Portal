#pragma once
#include <settings.h>

// Параметры датчика температуры. Лежат рядом с устройством, а не в ядре:
// добавление нового устройства не должно требовать правки общих файлов.
//
// Имена ключей неприкасаемы -- переименование меняет хэш и равносильно
// потере параметра.

namespace keys {

namespace sensor {
// Отдельной подписи показания больше нет: и портал, и сущность
// HomeAssistant называются общим именем устройства (dev.name). Ключ
// "sensor.label" из прошивок до 4.5.0 остаётся в базе неиспользованной
// ячейкой -- удалять её нечем, а ради полусотни байт сбрасывать все
// настройки миграцией нельзя.

// Период опроса датчика в секундах.
constexpr settings::Key refresh{"sensor.refresh"};
}  // namespace sensor

}  // namespace keys
