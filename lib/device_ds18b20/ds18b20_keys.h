#pragma once
#include <settings.h>

// Параметры датчика температуры. Лежат рядом с устройством, а не в ядре:
// добавление нового устройства не должно требовать правки общих файлов.
//
// Имена ключей неприкасаемы -- переименование меняет хэш и равносильно
// потере параметра.

namespace keys {

namespace sensor {
// Подпись показания в портале. В HomeAssistant сущность называется общим
// именем устройства (dev.name), которое подставляет ядро, поэтому здесь
// действительно только подпись, а не второе имя.
constexpr settings::Key label{"sensor.label"};

// Период опроса датчика в секундах.
constexpr settings::Key refresh{"sensor.refresh"};
}  // namespace sensor

}  // namespace keys
