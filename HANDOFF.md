# Handoff: поддержка восьмиканальной платы ESP-07S

Обновлено: 2026-08-06

## Цель

Добавить в этот репозиторий стабильную прошивку для соседней восьмиканальной
релейной платы на ESP8266 ESP-07S, сохранив существующие образы ESP-01, D1 mini
и DS18B20 совместимыми по поведению и MQTT-топикам.

Рабочая ветка: `feature/esp07s-relay8`.

## Область работы

- новый профиль платы `esp07s` с корректной разметкой 1 МБ и контролем OTA;
- новое окружение `esp07s_relay8` без адресов и секретов в репозитории;
- отдельная реализация устройства `device_relay_bank` для восьми каналов;
- несколько независимых сущностей Home Assistant на одном физическом устройстве;
- портал, подписи каналов, восстановление состояний и таймеры с выбором канала;
- native-тесты, сборки всех конечных образов, CI, release и runbook.

Вне области без отдельного разрешения: `Gate`, прошивка DS18B20 и любые
загрузки на физические устройства. Проверка на железе, когда до неё дойдём,
начинается с релейной платы.

## План и состояние

- [x] Осмотрена архитектура текущего репозитория и соседнего проекта.
- [x] Создана ветка `feature/esp07s-relay8`.
- [x] Создан этот журнал работы.
- [x] Добавить профиль `esp07s` и окружение `esp07s_relay8`.
- [x] Обобщить контракт устройства и MQTT-транспорт на несколько сущностей.
- [x] Сохранить прежнюю ветку построения MQTT-топиков одноканального реле и датчика.
- [x] Реализовать восьмиканальное устройство и его настройки.
- [x] Добавить целевой канал в суточные таймеры банка реле.
- [x] Добавить тесты и обновить архитектурную документацию, CI и release.
- [x] Выполнить `pio test -e native`.
- [x] Собрать все конечные образы и проверить каждую строку `OTA headroom`.
- [x] Подготовить чек-лист проверки релейной платы без фактической прошивки.
- [x] Исправить обнаруженное на OTA несоответствие Flash 4 МБ/1 МБ.
- [x] Пересобрать исправленную версию 4.0.1 и проверить заголовок BIN.

## Принятые решения

1. Соседний каталог называется `ESP32-Relay-Portal`, но текущая плата и код
   используют ESP8266 ESP-07S. Поддержка настоящего ESP32 в эту ветку не входит.
2. Восьмиканальный банк будет отдельным типом устройства, а не набором
   условных веток внутри стабильного одноканального `device_relay`.
3. MQTT должен представлять восемь стабильных сущностей с идентификаторами
   `relay_1` ... `relay_8`. Пользовательские подписи не должны участвовать в
   топиках или `unique_id`.
4. Состояние восьми каналов публикуется одним JSON в общем state topic;
   discovery и command topic у каждого канала свои, availability общий.
5. Для одноканальных устройств пути топиков, `unique_id` и `object_id` должны
   остаться прежними.
6. Физическая релейная плата сообщает реальный объём Flash 1 МБ. Профиль
   ESP-07S поэтому принудительно использует `eagle.flash.1m64.ld`: в отличие от
   старой разметки `1m` она сохраняет 64 КБ под используемый прошивкой LittleFS.

## Найденные трудности и риски

- Восьмиканальные изменения соседнего проекта находятся в незакоммиченной
  рабочей копии. Она используется только как источник распиновки и требований.
- Текущий `CoreMqttClient` хранит ровно одну подписку и один callback. Для
  восьми командных топиков потребуется ограниченный таблицей набор подписок.
- Текущий статический контракт описывает одну Home Assistant сущность. Его
  расширение затрагивает общее ядро, поэтому обязательны сборки всех образов.
- GPIO0 и GPIO15 являются загрузочными GPIO ESP8266. Корректность их внешних
  подтяжек определяется схемой платы и не может быть исправлена прошивкой.
- Старый восьмиканальный код создаёт восемь объектов с GPIO0 до `setup()` и
  таймерами управляет только первым реле. Эту реализацию переносить нельзя.
- Текущий образ реле занимает около 472 КБ. Для ESP-01 и ESP-07S с 1 МБ запас
  мал, поэтому даже небольшие изменения должны контролироваться обеими
  сборками.
- Во время работы отдельно изменился `tools/broker.ini.example` (`10.0.1.5` на
  `192.168.1.10`). Это не относится к реализации платы; изменение сохранено как
  пользовательское и не должно попасть в тематический коммит без решения владельца.
- `pio project config` разобрал новое окружение успешно, но завершение команды
  попыталось записать телеметрию в `~/.platformio/.cache`, что запрещено
  песочницей. Для итоговых запусков PlatformIO может потребоваться разрешение
  на его штатный домашний кеш; это не ошибка конфигурации проекта.

## Промежуточные проверки

- После расширения MQTT-контракта: `pio test -e native` -- 153/153 успешно.
- `esp01_relay` после расширения ядра: сборка успешна, RAM 34 616 байт,
  OTA 473 808 из 481 280 байт, осталось 7 472 байта.
- `esp07s_relay8`: первая сборка успешна, RAM 35 144 байта, OTA 475 296 из
  1 044 464 байт, осталось 569 168 байт. Эта проверка обнаружена как ошибочная:
  профиль платы описывал 4 МБ, а физический модуль имеет 1 МБ.

## Итоговые проверки версии 4.0.0 (заменена исправлением 4.0.1)

- `git diff --check` -- успешно.
- `pio test -e native` -- 157/157 успешно.
- `esp01_relay` -- 473 808 / 481 280, осталось 7 472 байта.
- `d1_mini_relay` -- 473 824 / 1 044 464, осталось 570 640 байт.
- `esp07s_relay8` -- 475 456 / 1 044 464, осталось 569 008 байт.
- `esp01_ds18b20` -- 471 728 / 481 280, осталось 9 552 байта.
- `esp01_relay_metrics` -- 474 448 / 481 280, осталось 6 832 байта.

## Исправление версии 4.0.1

- OTA-загрузка образа 4.0.0 была отклонена устройством: `420ERROR[9]: new Flash
  config wrong real: 1048576`.
- Причина: манифест PlatformIO для `esp07s` по умолчанию кодировал в заголовке
  образа 4 МБ, тогда как устройство обнаружило 1 МБ (1 048 576 байт).
- Профиль переведён на `eagle.flash.1m64.ld`, OTA-предел снижен до 481 280 байт.
- Версия поднята до 4.0.1, чтобы исправленный BIN не путался с образом 4.0.0.
- `esp01_relay` -- 473 808 / 481 280, осталось 7 472 байта.
- `d1_mini_relay` -- 473 824 / 1 044 464, осталось 570 640 байт.
- `esp07s_relay8` -- 475 456 / 481 280, осталось 5 824 байта.
- `esp01_ds18b20` -- 471 728 / 481 280, осталось 9 552 байта.
- `esp01_relay_metrics` -- 474 448 / 481 280, осталось 6 832 байта.
- Заголовок `ESP_Relay_ESP07S_8ch_v4.0.1.bin` начинается с `e9 02 00 20`;
  код размера Flash `2` соответствует 1 МБ в ESP8266 core.
- PlatformIO продолжает показывать `HARDWARE: ... 4MB Flash` из манифеста
  `esp07s`, но linker script, доступный размер 958 448 байт и заголовок BIN
  подтверждают фактическую конфигурацию 1 МБ.
- `pio test -e native` -- 157/157 успешно; `git diff --check` -- успешно.

На момент проверки 4.0.1 на физические устройства ничего не загружалось.

## Расширение таймеров версии 4.1.0

- В список целей таймера после реле 1–8 добавлен пункт `All relays` со
  значением 8. Существующие сохранённые значения 0–7 не меняют смысл.
- Групповая цель выполняет действие для всех восьми каналов за один проход
  расписания и одну последующую синхронизацию состояния/MQTT.
- Каналы в `Button mode` при групповой цели сохраняют прежнюю семантику и
  всегда получают импульс `On`; обычные каналы выполняют выбранные
  `On`, `Off` или `Toggle`.
- `pio test -e native` -- 160/160 успешно.
- `esp01_relay` -- 473 808 / 481 280, осталось 7 472 байта.
- `d1_mini_relay` -- 473 824 / 1 044 464, осталось 570 640 байт.
- `esp07s_relay8` -- 475 728 / 481 280, осталось 5 552 байта.
- `esp01_ds18b20` -- 471 728 / 481 280, осталось 9 552 байта.
- `esp01_relay_metrics` -- 474 448 / 481 280, осталось 6 832 байта.
- Заголовок ESP-07S остаётся `e9 02 00 20` (Flash 1 МБ).
- Аппаратная проверка группового таймера ещё не выполнялась; сценарий добавлен
  в `docs/runbooks/verification.md`.

## Загрузка на ESP-07S

- 2026-08-06 образ `ESP_Relay_ESP07S_8ch_v4.1.0.bin` успешно загружен по OTA
  с локальными параметрами из `platformio_override.ini`.
- OTA-загрузчик принял все 475 728 байт и вернул `Result: OK`.
- После перезагрузки портал отвечает `HTTP 200` и показывает версию `4.1.0`.
- Каналы реле после загрузки программно не переключались, чтобы не воздействовать
  на возможную подключённую нагрузку.
- `Gate` и DS18B20 не затрагивались.

## Следующий шаг

Выполнить аппаратный smoke-тест ESP-07S при безопасной нагрузке: проверить
каналы 1–8, новую цель `All relays` и холодный старт GPIO0/GPIO15. `Gate` и
DS18B20 в эту проверку не входят.

## Локальная массовая OTA-загрузка

- Создан локальный `tools/fleet_upload.py` с инвентарём релейных плат и
  датчиков; файл содержит адреса и добавлен в `.gitignore` целиком.
- Без `--execute` утилита только показывает план. Перед OTA она сначала
  собирает все нужные окружения, затем проверяет доступность и обновляет
  устройства последовательно, после каждого ожидая портал с новой версией.
- По умолчанию выбираются только реле. Для DS18B20 обязательно явно передать
  `--allow-sensors`; дополнительно проверяется отсутствие незакоммиченных
  изменений в коде прошивки.
- До версии 4.2.0 D1 mini с DS18B20 была внесена в локальный инвентарь как
  заблокированная цель: утилита не позволяла отправить на неё образ ESP-01.
- Dry-run группы реле, вывод инвентаря и защитные отказы для датчиков проверены;
  массовая загрузка утилитой не запускалась.

## Окружение D1 mini с DS18B20, версия 4.2.0

- Добавлено окружение `d1_mini_ds18b20` на профиле `[d1_mini]` с OneWire на
  D4/GPIO2 и отдельным образом `ESP_DS18B20_D1_mini_v4.2.0.bin`.
- `device_ds18b20` теперь требует явный `ONE_WIRE_PIN`: ESP-01 передаёт GPIO2,
  D1 mini — D4. Скрытого пина по умолчанию больше нет.
- Новый образ добавлен в CI, release workflow, архитектуру и runbook.
- Локальная fleet-утилита разблокировала D1 mini DS18B20, но массовая загрузка
  и загрузка на датчики не выполнялись.
- `pio test -e native` -- 160/160 успешно; `git diff --check` -- успешно.
- `esp01_relay` -- 473 808 / 481 280, осталось 7 472 байта.
- `esp01_relay_metrics` -- 474 448 / 481 280, осталось 6 832 байта.
- `d1_mini_relay` -- 473 824 / 1 044 464, осталось 570 640 байт.
- `esp07s_relay8` -- 475 728 / 481 280, осталось 5 552 байта.
- `esp01_ds18b20` -- 471 728 / 481 280, осталось 9 552 байта.
- `d1_mini_ds18b20` -- 471 728 / 1 044 464, осталось 572 736 байт.
- Заголовок D1 mini DS18B20 начинается с `e9 02 02 40` и кодирует штатные
  4 МБ Flash; ESP-01 DS18B20 остаётся `e9 02 00 20` с 1 МБ.
- Отдельная пользовательская правка `d1_mini_relay` с D2 на D1 сохранена в
  рабочем дереве и не относится к этому окружению; в тематический коммит её
  включать нельзя без отдельного решения владельца.
